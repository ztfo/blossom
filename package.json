{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "generate": "nuxt generate",
    "postgenerate": "touch .output/public/.nojekyll",
    "deploy": "npm run generate && npm run postgenerate && gh-pages -d .output/public",
    "dev": "nuxt dev",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.12.4",
    "pug": "^3.0.3",
    "pug-plain-loader": "^1.1.0",
    "vue": "latest"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
